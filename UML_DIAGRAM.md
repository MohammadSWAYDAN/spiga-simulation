# 📐 DIAGRAMME UML SIMPLIFIÉ - SPIGA

## 🎯 Hiérarchie des Classes (Héritage)

```
┌──────────────────────────────────────┐
│        ActifMobile (Abstract)        │
│  ─────────────────────────────────   │
│  - id: String                        │
│  - x, y, z: double                   │
│  - vitesseMax: double                │
│  - autonomieActuelle: double         │
│  - state: AssetState                 │
│  - targetX, targetY, targetZ: double │
│  - currentMission: Mission           │
│  ─────────────────────────────────   │
│  + update(dt: double)                │
│  + deplacer(x, y, z)                 │
│  + moveTowards(x, y, z, dt)          │
│  + updateBattery(dt)                 │
│  + recharger()                       │
│  + checkBatteryState()               │
│  + getState(): AssetState            │
│  + getAutonomieActuelle(): double    │
└──────────────────────────────────────┘
           ▲                ▲
         extends          extends
           │                │
    ┌──────┴──────┐    ┌────┴────────┐
    │             │    │             │
    │ ActifAerien │    │ ActifMarin  │
    │ (Abstract)  │    │ (Abstract)  │
    │ ─────────── │    │ ─────────── │
    │ - altitude  │    │ - profondeur│
    │   Min/Max   │    │   Min/Max   │
    │ ─────────── │    │ ─────────── │
    │ + deplacer()│    │ + deplacer()│
    └──────┬──────┘    └────┬────────┘
           │                │
      ┌────┴─────┬─────┐    │
      │           │     │    │
    ┌─┴──────────┐│     │    │
    │Drone       ├┘     │    │
    │ LogistiqueA│      │    │
    │ ttaque     │      │    │
    │            │ ┌────┴────┴────┬─────────┐
    │ Reconnai- │ │              │         │
    │ ssance    │ │              │         │
    │ Heli      ├─┴─────────────┐│ ┌──────┴────┐
    │ Glider    │               ││ │           │
    └───────────┘         ┌─────┴┴┘           │
                          │              ┌────┴──────┐
                    ┌─────┴─────┐        │            │
                    │            │     ┌─┴──────────┐│
            ┌───────┴─┐    ┌────┴────┐ │NavireP   ├─┘
            │          │    │         │ │atrouille │
    ┌───────┴────────┐ │ ┌──┴────────┴┐│ NavireLogistique
    │Vehicle        │ │ │            ││ NavireRecherche
    │ Surface       │ │ │ Vehicle    ├┘ └────────────────┘
    │ (Abstract)    ├─┘ │ SousMarin  │
    │               │   │ (Abstract)  │
    │ (surface only)│   │            │
    └───────────────┘   ├─ SousMarinAttaque
                        └─ SousMarinExploration
```

---

## 🔄 Interfaces et Contrats

```
┌────────────────────────────┐
│    << interface >>         │
│     Deplacable             │
│  ────────────────────────  │
│  + deplacer(x, y, z)       │
│  + calculerTrajet(x, y, z) │
└────┬───────────────────────┘
     │ implements
     │
┌────▼──────────────────┐
│   ActifMobile         │
└───────────────────────┘

            +

┌────────────────────────────┐
│    << interface >>         │
│     Rechargeable           │
│  ────────────────────────  │
│  + recharger()             │
│  + getAutonomieActuelle()  │
│  + getAutonomieMax()       │
└────┬───────────────────────┘
     │ implements
     │
┌────▼──────────────────┐
│   ActifMobile         │
└───────────────────────┘

            +

┌────────────────────────────┐
│    << interface >>         │
│     Pilotable              │
│  ────────────────────────  │
│  + demarrer()              │
│  + eteindre()              │
└────┬───────────────────────┘
     │ implements
     │
┌────▼──────────────────┐
│   ActifMobile         │
└───────────────────────┘

            +

┌────────────────────────────┐
│    << interface >>         │
│     Communicable           │
│  ────────────────────────  │
│  + envoyerMessage(msg)     │
│  + recevoirOrdre(ordre)    │
└────┬───────────────────────┘
     │ implements
     │
┌────▼──────────────────┐
│   ActifMobile         │
└───────────────────────┘

            +

┌────────────────────────────┐
│    << interface >>         │
│     Alertable              │
│  ────────────────────────  │
│  + genererAlerte(msg)      │
│  + recevoirAlerte(alert)   │
└────┬───────────────────────┘
     │ implements
     │
┌────▼──────────────────┐
│   ActifMobile         │
└───────────────────────┘
```

---

## 🎯 Composition (Has-A Relationships)

```
┌─────────────────────────────┐
│    GestionnaireEssaim       │
│  ───────────────────────── │
│  - flotte: List<ActifMobile>│◄─────┐
└────┬────────────────────────┘      │
     │ contains many              has│
     │                               │
     └────────────────────────────────┘
                                    ┌───────────────┐
                                    │ ActifMobile   │
                                    └─────────┬─────┘
                                              │
                                          has │
                                              │
                                    ┌─────────▼──────────┐
                                    │   Mission          │
                                    │  ───────────────── │
                                    │  - id: String      │
                                    │  - titre: String   │
                                    │  - type: MissionType│
                                    │  - statut: Status  │
                                    │  - targetX, Y, Z   │
                                    │  ───────────────── │
                                    │  + execute()       │
                                    │  + complete()      │
                                    │  + fail()          │
                                    └────────────────────┘

┌─────────────────────────────┐
│  SimulationService          │
│  ───────────────────────── │
│  - gestionnaire: Gestionnaire│◄─────┐ references
│  - obstacles: List<Obstacle>│       │
│  - weather: Weather         │       │
│  - zone: ZoneOperation      │       │
│  ───────────────────────── │   uses│
│  + handle(now: long)        │       │
│  + updateSimulation(dt)     │───────┘
│  + startSimulation()        │
│  + stopSimulation()         │
└─────────────────────────────┘

┌─────────────────────────────┐
│  MapCanvas                  │
│  ───────────────────────── │
│  - canvas: Canvas           │
│  - zone: ZoneOperation      │
│  - selectedActifs: List     │
│  ───────────────────────── │
│  + draw(actifs, obstacles)  │
│  + getActifAt(x, y)         │
│  + worldToScreenX(x)        │
│  + handleMapClicked(x, y)   │
└─────────────────────────────┘

┌─────────────────────────────┐
│  MainController             │
│  ───────────────────────── │
│  - gestionnaire: Gestionnaire│◄────┐
│  - simulationService: Service│◄────├─ creates and
│  - mapCanvas: MapCanvas     │◄────├─ manages
│  - sideViewCanvas: Canvas   │◄────┘
│  - zone: ZoneOperation      │
│  ───────────────────────── │
│  + initialize()             │
│  + handleMapClicked(x, y)   │
│  + updateUI()               │
│  + handleSelectionChanged() │
└─────────────────────────────┘
```

---

## 📊 Énumérations (Enums)

```
┌──────────────────────────────┐
│  EtatOperationnel            │
│  (in ActifMobile)            │
├──────────────────────────────┤
│  • AU_SOL                    │
│  • EN_MISSION                │
│  • EN_MAINTENANCE            │
│  • EN_PANNE                  │
└──────────────────────────────┘

┌──────────────────────────────┐
│  AssetState                  │
│  (in ActifMobile)            │
├──────────────────────────────┤
│  • IDLE                      │
│  • MOVING_TO_TARGET          │
│  • EXECUTING_MISSION         │
│  • LOW_BATTERY               │
│  • RETURNING_TO_BASE         │
│  • STOPPED                   │
│  • RECHARGING                │
└──────────────────────────────┘

┌──────────────────────────────┐
│  MissionType                 │
│  (in Mission)                │
├──────────────────────────────┤
│  • SURVEILLANCE              │
│  • LOGISTICS                 │
│  • NAVIGATION                │
│  • SEARCH_AND_RESCUE         │
└──────────────────────────────┘

┌──────────────────────────────┐
│  StatutMission               │
│  (in Mission)                │
├──────────────────────────────┤
│  • PLANIFIEE                 │
│  • EN_COURS                  │
│  • TERMINEE                  │
│  • ECHOUEE                   │
│  • ANNULEE                   │
└──────────────────────────────┘
```

---

## 🔀 Classe Mission et Sous-Classes

```
┌─────────────────────────────────┐
│  << abstract >>                 │
│        Mission                  │
│  ───────────────────────────── │
│  # id: String                  │
│  # titre: String               │
│  # type: MissionType           │
│  # statut: StatutMission       │
│  # targetX, targetY, targetZ   │
│  # startTime, endTime: long    │
│  ───────────────────────────── │
│  + execute(): abstract void    │
│  + complete(): void            │
│  + fail(): void                │
│  + getStatut(): StatutMission  │
└──────────┬──────────────────────┘
           │ extends
    ┌──────┼──────────────────┬──────────────┐
    │      │                  │              │
┌───▼──────┴──┐  ┌──────────┴────┐ ┌───────┴──────────────┐
│ Mission     │  │ Mission        │ │ Mission             │
│ Logistique  │  │ Surveillance   │ │ Recherche Et        │
│             │  │ Maritime       │ │ Sauvetage           │
│ - cargo     │  │                │ │                     │
│ - destination  │ - zone: Zone   │ │ - coordinatesVictime│
│ ─────────── │  │ ─────────────  │ │ ─────────────────── │
│ + execute() │  │ + execute()    │ │ + execute()         │
└─────────────┘  └────────────────┘ └─────────────────────┘
```

---

## 🗺️ Package Organization

```
com.spiga
│
├── Main.java
│   └── Lance l'application
│
├── core/
│   ├── ActifMobile (abstract)
│   │   ├── ActifAerien (abstract)
│   │   │   ├── DroneLogistique
│   │   │   ├── DroneAttaque
│   │   │   ├── DroneReconnaissance
│   │   │   ├── HelicoptereSauvetage
│   │   │   └── GliderOceanographique
│   │   │
│   │   └── ActifMarin (abstract)
│   │       ├── VehiculeSurface (abstract)
│   │       │   ├── NavirePatrouille
│   │       │   ├── NavireLogistique
│   │       │   └── NavireRecherche
│   │       │
│   │       └── VehiculeSousMarin (abstract)
│   │           ├── SousMarinAttaque
│   │           └── SousMarinExploration
│   │
│   ├── Interfaces/
│   │   ├── Deplacable
│   │   ├── Rechargeable
│   │   ├── Communicable
│   │   ├── Pilotable
│   │   └── Alertable
│   │
│   └── SimulationService
│
├── management/
│   ├── GestionnaireEssaim
│   ├── Mission (abstract)
│   │   ├── MissionLogistique
│   │   ├── MissionSurveillanceMaritime
│   │   └── MissionRechercheEtSauvetage
│   │
│   └── MissionSurveillanceMaritime
│
├── environment/
│   ├── ZoneOperation
│   ├── Obstacle
│   └── Weather
│
└── ui/
    ├── MainView.fxml
    ├── MainController
    ├── MapCanvas
    ├── SideViewCanvas
    ├── SidebarController
    ├── MissionController
    └── [autres panneaux FXML]
```

---

## 🔄 Flux de Dépendances

```
     Main
      │
      ├─ load → MainView.fxml
      │
      └─ create → MainController
                  │
                  ├─ create → GestionnaireEssaim
                  │             │
                  │             └─ store → List<ActifMobile>
                  │
                  ├─ create → SimulationService
                  │             │
                  │             ├─ reference → GestionnaireEssaim
                  │             ├─ create → List<Obstacle>
                  │             ├─ create → Weather
                  │             └─ reference → ZoneOperation
                  │
                  ├─ create → MapCanvas
                  │             │
                  │             └─ reference → ZoneOperation
                  │
                  ├─ create → SideViewCanvas
                  │
                  ├─ inject → SidebarController
                  │             │
                  │             ├─ reference → GestionnaireEssaim
                  │             └─ reference → MainController
                  │
                  └─ inject → MissionController
                              │
                              ├─ reference → GestionnaireEssaim
                              └─ reference → MainController
```

---

## 📈 Cycle de Vie Complet

```
1. APPLICATION STARTS
   ↓
2. Main.start()
   ├─ Load MainView.fxml
   └─ Create MainController instance
   ↓
3. MainController.initialize()
   ├─ Create GestionnaireEssaim (empty)
   ├─ Create SimulationService
   ├─ Create MapCanvas + SideViewCanvas
   ├─ Wire SidebarController + MissionController
   └─ Start SimulationService (60 FPS loop)
   ↓
4. SimulationService Loop (CONTINUOUS, 60x per second)
   ├─ For each ActifMobile in GestionnaireEssaim:
   │  └─ Call actif.update(dt)
   │     ├─ moveTowards(target)
   │     ├─ updateBattery(dt)
   │     └─ checkBatteryState()
   │
   ├─ Update Weather (every 30s)
   │
   ├─ Handle Collisions
   │
   └─ MainController.updateUI()
      ├─ MapCanvas.draw(actifs, obstacles)
      └─ SideViewCanvas.draw(actifs)
   ↓
5. USER INTERACTIONS
   ├─ Click on Map → handleMapClicked()
   ├─ Select Asset → handleSelectionChanged()
   ├─ Create Mission → MissionController.createMission()
   └─ Assign Mission → GestionnaireEssaim.demarrerMission()
   ↓
6. Loop continues...
```

---

## 🎯 Résumé Patterns OOP

| Pattern | Où | Raison |
|---------|-----|--------|
| **Héritage** | ActifMobile ← ActifAerien ← DroneLogistique | Réutiliser code commun |
| **Interface** | Deplacable, Rechargeable, etc. | Définir contrats |
| **Abstrait** | ActifMobile, Mission | Forcer implémentation dans enfants |
| **Polymorphisme** | update() appelé sur tous les actifs | Chaque type avec sa logique |
| **Composition** | GestionnaireEssaim contient List<Actif> | Gérer groupe d'objets |
| **Enum** | AssetState, MissionType | États et types sûrs |
| **Generic** | List<ActifMobile> | Type-safe collections |

---

**Ce diagramme devrait vous aider à visualiser la structure ! 📐**
